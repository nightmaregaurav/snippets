sudo pacman -S timeshift
sudo timeshift --gui
enable home dir backup
disable /timeshift/** dir backup
take backup as fresh install backup

sudo pacman-mirrors -f

sudo pacman -Rs firefox firefox-gnome-theme-maia
sudo pacman -S firefox-developer-edition gvfs gvfs-mtp

sudo ln -s /usr/bin/firefox-developer-edition /usr/bin/firefox
git config --global credential.helper store

mkdir ~/Projects
cd ~/Projects
git clone https://github.com/nightmaregaurav/snippets.git
cd ..

install sublime-text-4 from AUR

cleanup

sudo pacman -Syyyu
sudo pacman -S libva-mesa-driver opencl-driver opencl-headers gst-libav gst-plugin-libcamera libcamera-tools pipewire-audio malcontent manjaro-printer linux-firmware-qlogic nodejs npm yarn jdk-openjdk python-pip firefox-developer-edition composer flameshot bamf appmenu-gtk-module libkeybinder3 libdbusmenu-gtk2 libdbusmenu-gtk3 libdbusmenu-qt5 sqlitebrowser pavucontrol-qt simplescreenrecorder telegram-desktop virt-manager screenkey
sudo pacman -Scc
sudo paccache -r

add keyboard language

apache setup
webserver permission setup

install freedownloadmanager from AUR
install obs-studio-git from AUR
install postman-bin from AUR

dotnet install

pip install virtualenvwrapper
composer global require laravel/installer

install jetbrains-toolbox
setup jetbrains-toolbox without login
install ides

echo "" >> ~/.bashrc
echo "export WORKON_HOME=$HOME/.virtualenvs" >> ~/.bashrc
echo "export PROJECT_HOME=$HOME/Projects" >> ~/.bashrc
echo "source $HOME/.local/bin/virtualenvwrapper.sh" >> ~/.bashrc
echo "export DOTNET_ROOT=$HOME/.dotnet" >> ~/.bashrc
echo "export PATH=$PATH:$HOME/.dotnet:$HOME/.dotnet/tools:$HOME/.config/composer/vendor/bin:$HOME/.local/share/JetBrains/Toolbox/scripts" >> ~/.bashrc

echo "" >> ~/.zshrc
echo "export WORKON_HOME=$HOME/.virtualenvs" >> ~/.zshrc
echo "export PROJECT_HOME=$HOME/Projects" >> ~/.zshrc
echo "source $HOME/.local/bin/virtualenvwrapper.sh" >> ~/.zshrc
echo "export DOTNET_ROOT=$HOME/.dotnet" >> ~/.zshrc
echo "export PATH=$PATH:$HOME/.dotnet:$HOME/.dotnet/tools:$HOME/.config/composer/vendor/bin:$HOME/.local/share/JetBrains/Toolbox/scripts" >> ~/.zshrc

dotnet cert setup

check and install firmwares from manjaro settings
systemctl enable --now nvidia-resume.service
nvidia-modprobe

sudo pacman -Syyyu
sudo pacman -Scc
sudo paccache -r

Remove Super+S shortcut
Remove Super+A shortcut
Remove Super+R shortcut
Remove Super+V shortcut
Remove PrintScr shortcut

Add extensions:
	Clipman simple clipboard manager:
		hotkey Super + V
	Search Light:
		hotkey Super + R
	AppIndicator and KStatus....  -> On
	Dash to Dock -> Install and on and setup
	Pmac update indicator -> on
	Removable drive menu -> on
	User theme -> on
	Arc menu:
		Hotkey Super
		Hotkey Runner Super + S
		Show activities button off
		Menu layout Redmond
		Menu Button = Set arch icon
		Menu Button Hover BC on
		Menu Button Hover FC on
		Menu Button Active BC on
		Menu Button Active FC on
		Menu Button Border radius 20
	Compiz alike magic lamp effect -> on
	Blur my shell:
		Sigma = 2
		Brightness = 0.94
		Color and noise = off
		Hack level = High Quality
	Quick settings Tweaker:
		Input> Always show input = True
	GTK Title Bar:
		Primary monitor only = off
		Hide when = maximized
	Hide Top Bar:
		Show panel when mouse approaches edge of the screen: on
		In ...... also show panel when full screen: on
		Show panel in overview: on
		Keep Hot corner sensetive even in hidden state: on
		Intellihide > Hide panel when a window takes the space: on
	Unite:
		Extend top bar left box: on
		Enable titlebar actions on top bar click
		Hide activities button: auto
		Hide win title bars: maximized
		Show window title in app menu: Maximized
		Show window buttons in top bar: Maximized
		Notification message position: Right
		Hide Appmenu Application icon: on
		Reduce top bar items spacing: on
		Top bar win buttons theme McMojave
	Lock keys:
		Indicator style: Show/Hide
		NOtifications: OSD

add virtual sink and camera
flameshot config

Add Alt+f4 to Close Window
Add Super+A to Show all apps
Add Super+E to launch nautilus
Add Ctrl+Alt+T to launch Terminal
Add PrintScr to launch 'flameshot gui'
Add Super+Tab to Switch Applications
Add Alt+Tab to Switch Windows
Add Super+Space to Maximize window
Add Super+Enter to Hide window
Add Super+M to Hide all windows
Add Global AppMenu

Take full timeshift backup as App Installations

cd ~/Downloads
git clone https://github.com/vinceliuice/WhiteSur-gtk-theme.git
cd WhiteSur-gtk-theme
./install.sh -m -t all -l -N glassy --normal --round --black
sudo ./tweaks.sh -d -g

Download icons from https://www.pling.com/p/1305429/ and install to .icons
Download icons from https://www.pling.com/p/1355701/ and install to .themes
Set theme and icons from in user theme extension and tweaks

Firefox userchrome and UI polishing
Settings polishing
Apps setup
Files restore from file backups